enum Choice {
    variant Left[value: i64],
    variant Right[value: i64],
}
fn main() -> i64 {
    let choice = Choice::Right(42);
    return match choice {
        Choice::Left[value] => value,
    };
}
