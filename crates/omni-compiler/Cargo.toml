[package]
name = "omni-compiler"
version = "0.2.0"
edition = "2021"
authors = ["Shreyash Jagtap <ssjagtap2016@gmail.com>"]
license = "Apache-2.0"

[lib]
name = "omni_compiler"
path = "src/lib.rs"
[package.metadata.omni]
project-version = "0.2.0.0"
cargo-semver-base = "0.2.0"
version-scheme = "stable.major.minor.patch"

[dependencies]
polonius_engine_adapter = { path = "../polonius_engine_adapter", optional = true }
lir = { path = "../lir" }
codegen-cranelift = { path = "../codegen-cranelift", optional = true, features = ["use_cranelift"] }
codegen-native = { path = "../codegen-native" }
codegen-wasm = { path = "../codegen-wasm", optional = true }
codegen-llvm = { path = "../codegen-llvm", optional = true }
omni-stdlib = { path = "../omni-stdlib" }
thiserror = "1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[features]
# Compatibility feature for the archived ownership oracle. The live v0.1.4
# adapter fails closed; full ownership checking is a v0.2.0 milestone.
ownership-oracle = ["dep:polonius_engine_adapter"]
use_polonius = ["ownership-oracle"]

# Feature to enable the incremental Salsa-based LSP database and tests
use_salsa_lsp = ["salsa"]
dev-jit = ["dep:codegen-cranelift"]
wasm-backend = ["dep:codegen-wasm"]
use_llvm = ["dep:codegen-llvm"]

[dependencies.salsa]
version = "0.26"
optional = true
features = ["macros"]

[dev-dependencies]
tempfile = "3"
