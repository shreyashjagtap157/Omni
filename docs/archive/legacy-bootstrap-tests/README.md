# Archived legacy bootstrap tests

These files exercised historical interpreter/JIT or prototype-stdlib paths. They are preserved for archaeology
and future migration, but are intentionally excluded from the v0.1.2 qualified native test surface because the
features they imply are not end-to-end native Omni semantics at this milestone.
