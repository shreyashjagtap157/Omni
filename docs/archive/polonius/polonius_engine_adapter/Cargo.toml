[package]
name = "polonius_engine_adapter"
version = "0.2.0-archived"
edition = "2021"
[package.metadata.omni]
project-version = "0.3.0.1"
cargo-semver-base = "0.3.0"
version-scheme = "stable.major.minor.patch"

[dependencies]

[features]
default = []
# Retained only so historical invocations produce an honest runtime error rather
# than a Cargo feature-resolution failure. No upstream Polonius dependency is
# pulled into the v0.1.4 baseline.
use_polonius_lib = []

# [ARCHIVED] This crate is historical; not qualified in v0.1.4.x. Ownership-sensitive MIR reserved for v0.2.0 milestone.
OWNERSHIP_ORACLE_QUALIFIED = False
