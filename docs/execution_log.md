# Historical execution-log redirect

The former execution log is archived at `archive/historical-plans/execution_log.md`.
Historical logs do not prove the current checkout builds. Current promotion evidence is
recorded in `../SOURCE_QUALIFICATION.json`, `../RELEASE_MANIFEST.json`, and the output of
`scripts/qualify-release.sh` / `.ps1`.
