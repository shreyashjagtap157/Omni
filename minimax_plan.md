# Historical plan redirect

The former `minimax_plan.md` is archived at
`docs/archive/historical-plans/minimax_plan.md`.

It is **not authoritative for the current implementation**. Use, in order:

1. `spec/README.md` and `spec/edition1/` for the language-definition target;
2. `docs/CURRENT_IMPLEMENTATION_MATRIX.md` for what the current compiler actually supports;
3. `docs/VERSIONING_AND_BOOTSTRAP_PLAN.md` for milestone sequencing;
4. `docs/LINEAGE_REMEDIATION_AUDIT_0.0.1_TO_0.1.2.md` for repaired historical debt;
5. `scripts/qualify-release.sh` / `.ps1` for executable promotion gates.

Historical documents are evidence and context only. They never override current source,
current normative specifications, or executable qualification results.
